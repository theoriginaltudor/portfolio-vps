server {
    listen 80;
    server_name _;

    location /images/ {
        alias /usr/share/nginx/html/images/;
        autoindex on;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}